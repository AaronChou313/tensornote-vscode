{
  "version": "0.2.0",
  "configurations": [
    {
      "name": "Run TensorNote Extension",
      "type": "extensionHost",
      "request": "launch",
      "args": ["--extensionDevelopmentPath=${workspaceFolder}"],
      "outFiles": ["${workspaceFolder}/dist/**/*.js"],
      "preLaunchTask": "TensorNote: Build"
    }
  ]
}
